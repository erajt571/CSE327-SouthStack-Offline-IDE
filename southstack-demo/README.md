# SouthStack Demo (legacy folder)

**Merged into [`../southstack-p2p/`](../southstack-p2p/).** [`index.html`](index.html) redirects there.

Use the unified app and open DevTools:

- `await ask("Write a simple Express server")` — same behavior as this demo’s `ask()` (user message only, streamed to console)
- `await promptCoding("…")` — coding-focused assistant with system prompt

See `southstack-p2p/README.md` for the full feature list.
